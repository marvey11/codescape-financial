{
  "private": "true",
  "name": "@csfin/dbinit",
  "version": "0.0.1",
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint --color --ext .ts --ext .tsx src/",
    "lint:fix": "eslint --color --fix --ext .ts --ext .tsx src/",
    "upload-securities": "ts-node src/uploadSecurities.ts",
    "upload-transactions": "ts-node src/uploadTransactions.ts"
  },
  "dependencies": {
    "@csfin/codi": "^0.0.1",
    "@csfin/core": "^0.0.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.1"
  }
}
